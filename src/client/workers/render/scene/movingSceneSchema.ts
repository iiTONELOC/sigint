export enum MovingSceneSchema {
  AttributeStride = 2,
}

export enum MovingSceneMotionPositionSchema {
  MotionPositionStride = 2,
}

export enum MovingSceneMotionPosition {
  Longitude = 0,
  Latitude = 1,
}

export enum MovingSceneAttribute {
  DirectionDegrees = 0,
  SpeedMetersPerSecond = 1,
}
