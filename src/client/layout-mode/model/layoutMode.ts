export enum LayoutMode {
  Auto = "auto",
  Mobile = "mobile",
  Desktop = "desktop",
}

export enum DeviceType {
  Phone = "phone",
  Tablet = "tablet",
  Desktop = "desktop",
}

export enum ViewportOrientation {
  Portrait = "portrait",
  Landscape = "landscape",
}
