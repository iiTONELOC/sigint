// ── Canvas-only constants ─────────────────────────────────────────────
// All UI components now use Tailwind classes with CSS vars from globals.css.
// These constants exist only for Canvas 2D ctx.font usage in GlobeVisualization.

/** Canvas font size in px for label rendering */
export const CANVAS_FONT_PX = 11;
