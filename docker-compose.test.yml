# Run headless E2E tests in a clean container:
#   docker compose -f docker-compose.test.yml up --build --abort-on-container-exit
#
# Host commands own TypeScript and Bun unit tests.

services:
  test:
    build:
      context: .
      dockerfile: Dockerfile.e2e
    environment:
      - NODE_ENV=test
